package com.apptolast.invernaderos.features.user

// Compatibility shim while legacy repositories and controllers still import the old package.
typealias User = com.apptolast.invernaderos.features.user.infrastructure.adapter.output.User
